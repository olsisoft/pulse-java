# Contributing to pulse-java

This repository is **a synchronised mirror** of `pulse-java/` in the
[streamflow monorepo](https://github.com/olsisoft/pulse-java).

Open issues + pull requests against the monorepo, not against this
repo. The sync workflow lifts code from there into here on every
streamflow `dev` push that touches `pulse-java/`.
